<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - ClinicaWEB by Buleanitos</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
        @*@Styles.Render("~/Content/css")*@ <!--bundle original-->
        <link href="/Content/Slate.css" rel="stylesheet"/><!--www.http://bootswatch.com/slate/-->
        <link href="/Content/bootswatch.min.css" rel="stylesheet"/><!-- bootstrap minified -->
        <!--@Styles.Render("~/Content/bootstrap")--> <!--bundle nuevo. No sé por qué no anda.-->
        @Scripts.Render("~/bundles/modernizr")
    </head>
    <body>
        @*<header>
            <div class="content-wrapper">
                <div class="float-left">
                    <p class="site-title">@Html.ActionLink("logo", "Index", "Home")</p>
                </div>
                <div class="float-right">
                    <section id="login">
                        @Html.Partial("_LoginPartial")
                    </section>
                    <!--<nav>
                        <ul id="menu">
                            <li>@Html.ActionLink("Home", "Index", "Home")</li>
                            <li>@Html.ActionLink("About", "About", "Home")</li>
                            <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                        </ul>
                    </nav>-->
                </div>
            </div>
        </header>*@
        <div class="navbar navbar-default navbar-fixed-top"> <!--navbar-inverse para que sea en gris-->
			<!-- Brand and toggle get grouped for better mobile display -->
            <div class="container">
			    <div class="navbar-header">
				    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
					    <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span>
					    <span class="icon-bar"></span> <span class="icon-bar"></span>
				    </button>
				    <b><a class="navbar-brand" @Html.ActionLink("ClinicaWeb  >", "Index", "Home")</a></b>
			    </div>
			    <center>
				    <div class="navbar-brand">@ViewBag.Title</div>
			    </center>
			    <!-- Collect the nav links, forms, and other content for toggling -->
			    @*@Html.Partial("_LoginPartial")*@ <!-- modificado -->
            </div>
		</div>
        
        <div class="container" style="margin-right: 15px; margin-left: 15px; padding-left: 0px; padding-right: 0px">
            <div class="page-header">
                <div class="row">
                    <div class="col-md-3">
                        @Html.Partial("Menu")
                    </div>
                    <div class="col-md-9">
                        @RenderSection("featured", required: false)
                        @RenderBody()
                    </div>
                </div>
            </div>
        </div>
        <footer>
            <div class="content-wrapper">
                <div class="float-left">
                    <p>&copy; @DateTime.Now.Year - Buleanitos</p>
                </div>
            </div>
        </footer>

        <!--<script type = "text/javascript" src="../../Scripts/jquery-ui-1.8.20.min.js"></script>-->
        
        @Scripts.Render("~/bundles/jquery")
        @RenderSection("scripts", required: false)
    </body>
</html>
